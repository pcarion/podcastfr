name: "Issue to add a podcast"
on:
  schedule:
    - cron: "15 0 * * *"
jobs:
  issue_add_podcast:
    runs-on: ubuntu-latest
    steps:
      - name: parse issue for new podcast addition
        uses: pcarion/awesome-podcasts-bot@v0.2
        with:
          repo-token: ${{ secrets.GH_PAT }}

